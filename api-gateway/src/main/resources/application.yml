server:
  port: 8080
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

spring:
  application:
    name: api-gateway
  config:
    import: "configserver:"
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true  # ??? ?????? ?? ???? ???? ????? ??
              lower-case-service-id: true
          routes:
            # User Service Routes
            - id : user-service
              uri: lb://user-service
              predicates:
                - Path=/api/v1/users/**
            # Game Service Routes
            - id: game-service
              uri: lb://game-service
              predicates:
                - Path=/api/v1/games/**
            # Point Service Routes
            - id: point-service
              uri: lb://point-service
              predicates:
                - Path=/api/v1/points/**
            # Betting Service Routes
            - id: betting-service
              uri: lb://betting-service
              predicates:
                - Path=/api/v1/bets/**

management:
  endpoints:
    web:
      exposure:
        include: refresh

#jwt:
#  secret: ${JWT_SECRET}
#  expiration: ${JWT_EXPIRATION}
