server:
  port: 0

spring:
  application:
    name: point-service
  datasource:
    url: jdbc:postgresql://localhost:5432/oneonone?currentSchema=points
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: points

management:
  endpoints:
    web:
      exposure:
        include: refresh