server:
  port: 0

spring:
  application:
    name: point-service
  config:
    import: "configserver:"


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
